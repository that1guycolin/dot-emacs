# -*- mode: snippet -*-
# name: usage-for-functions
# key: funcuse
# --

usage() {
    cat <<EOF
USAGE: \$\{funcstack[2]\} [OPTIONS] ${1:required-arguments}

${2:description}

OPTIONS:
  ${3:flag}
      ${4:flag-description}

  -h, --help
      Display this message and exit.

EOF
}