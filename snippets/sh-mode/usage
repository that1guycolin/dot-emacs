# -*- mode: snippet -*-
# name: usage
# key: usage
# --

usage() {
    cat <<EOF
USAGE: \$\{0##*/\} [OPTIONS] ${1:required-arguments}

${2:description}

OPTIONS:
  ${3:flag}
      ${4:flag-description}

  -h, --help
      Display this message and exit.

EOF
}
